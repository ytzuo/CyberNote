<Application x:Class="CyberNote.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CyberNote"
             xmlns:converters="clr-namespace:CyberNote.Converters">
    <Application.Resources>
        <!-- 主题颜色资源 (默认绿色，浅色模式) -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#45A049"/>
        <SolidColorBrush x:Key="PrimaryPressedBrush" Color="#3D8B40"/>
        
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PopupBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#888888"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#DDDDDD"/>

        <!-- Active Card Background (Default Light Greenish Gradient fallback or solid) -->
        <SolidColorBrush x:Key="ActiveCardBackgroundBrush" Color="#F4FFF6"/>

        <!-- Ghost Button Colors (浅色 hover/pressed) -->
        <SolidColorBrush x:Key="GhostButtonHoverBrush" Color="#FFE8F5E9"/>
        <SolidColorBrush x:Key="GhostButtonPressedBrush" Color="#FFC8E6C9"/>
        
        <!-- 细窄滚动条样式（更明显，悬停仅改变颜色，颜色更浅以适配白色背景） -->
        <Style TargetType="ScrollBar" x:Key="ThinScrollBar">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#606060"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <!-- 轨道：浅灰色更明显于白色背景 -->
                            <Border Background="#FFF5F5F5" CornerRadius="6"/>
                            <Track x:Name="PART_Track" Orientation="{TemplateBinding Orientation}" IsDirectionReversed="True" Focusable="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Background="Transparent" BorderThickness="0" IsTabStop="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Background="#EEEEEE" Opacity="1" MinHeight="30" MinWidth="12" Margin="2" HorizontalAlignment="Stretch">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="ThumbBorder" Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#999999" BorderThickness="1">
                                                    <Border.Effect>
                                                        <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.12"/>
                                                    </Border.Effect>
                                                </Border>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Background="Transparent" BorderThickness="0" IsTabStop="False"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 全局 ScrollViewer 样式: 使用双列布局避免内容被覆盖；无滚动条时去掉右侧内容内边距统一勾选框位置 -->
        <Style TargetType="ScrollViewer" x:Key="GlobalScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Column="0" Margin="0,0,4,0"/>
                            <ScrollBar Grid.Column="1" Name="PART_VerticalScrollBar"
                                       Orientation="Vertical"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{StaticResource ThinScrollBar}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ComputedVerticalScrollBarVisibility" Value="Collapsed">
                                <Setter TargetName="PART_ScrollContentPresenter" Property="Margin" Value="0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 应用全局样式 -->
        <Style TargetType="ScrollViewer" BasedOn="{StaticResource GlobalScrollViewer}"/>
    </Application.Resources>
</Application>
